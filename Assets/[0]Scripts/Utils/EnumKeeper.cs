namespace SodaBoom.Utils
{
    public enum WeaponType : byte
    {
        Soda,
        Mentos
    }

    public enum EnemyState : byte
    {
        Empty,
        Full,
        Destroyed,
        Stopped
    }

    public enum EnemyType : byte
    {
        Can,
        Bottle
    }
}